import "../styles/Header.css"

const Header = ({heading}) => {
    return (
        <>
            <header></header>

            <section class="heading">
                <h1>{heading}</h1>
            </section>
        </>
    )
}

export default Header;